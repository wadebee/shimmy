# AWS CLI

`tool.conf` selects version 2.31. Its runtime mounts `$PWD`, mounts `~/.aws`
read-only when present, and forwards upstream `AWS_*` variables.

## Child contexts

- [version 2.31](versions/2.31/CONTEXT.md)
