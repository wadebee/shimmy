# GitHub CLI 2.94 runtime

`run.sh` selects an override or the local image built from `container/`.
`smoke.conf` defines installed smoke behavior.

## Child contexts

- [container build context](container/CONTEXT.md)
