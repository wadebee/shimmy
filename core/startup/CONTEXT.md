# Shell startup integration

`startup.sh` renders, adds, and removes the managed activation block for
supported POSIX-oriented shells. It must only change explicitly selected
startup files and remain idempotent.
