# Escalation skill

`SKILL.md` defines the narrow Podman and Shimmy-wrapper approval workflow for
AI-agent environments. Installed wrapper discovery follows the currently
activated profile-local `bin/` directory; there is no shared dispatcher root
or installed profile selector.
