# Core agent skills

These skills cover Shimmy installation, initialization, escalation, tool
creation, and local image builds. Each skill directory owns `SKILL.md` without
a leaf context file. The `agent/core/` name denotes core management guidance
and is intentionally unchanged by the shared-module rename from `core/` to
`lib/`.
