name: Shim Tests

on:
  pull_request:
    paths:
      - "bootstrap.sh"
      - "commands/**"
      - "lib/**"
      - "tests/**"
      - "tools/**"
      - ".agents/skills/**"
      - ".pre-commit-config.yaml"
      - ".github/workflows/test.yml"
      - "CONTRIBUTING.md"
      - "AGENTS.md"
      - "BOOTSTRAP.md"
      - "README.md"
      - "docs/**"
      - "plugins/shimmy/**"
  push:
    paths:
      - "bootstrap.sh"
      - "commands/**"
      - "lib/**"
      - "tests/**"
      - "tools/**"
      - ".agents/skills/**"
      - ".pre-commit-config.yaml"
      - ".github/workflows/test.yml"
      - "CONTRIBUTING.md"
      - "AGENTS.md"
      - "BOOTSTRAP.md"
      - "README.md"
      - "docs/**"
      - "plugins/shimmy/**"
  workflow_dispatch:

jobs:
  shim-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check Podman Dependency
        run: podman --version
        
      - name: Remove github runner jq to force shim usage
        run: sudo rm /usr/bin/jq || true

      - name: Run shim tests
        run: ./tests/test.sh
