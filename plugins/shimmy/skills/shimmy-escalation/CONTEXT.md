# Escalation skill

`SKILL.md` defines the narrow Podman and Shimmy-wrapper approval workflow for
AI-agent environments. Installed wrapper discovery follows the profile-local
`bin/` directory currently selected on `PATH`; there is no shared dispatcher
root or installed profile selector.
